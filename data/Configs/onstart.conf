# sync time
exec timedatectl set-ntp 1

# wallpaper
exec nitrogen --restore

# keyboard layout switching 
exec_always setxkbmap -layout us,ru -option grp:win_space_toggle

# bar
exec_always $HOME/.config/polybar/launch.sh --grayblocks

# changing opacity
#exec xcompmgr &
#exec devilspie ~/.devilspie/main.dp -a &
